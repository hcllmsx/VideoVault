<!DOCTYPE html>
<html lang="zh">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'VideoVault' %>
    </title>
    <!-- Bootstrap 5 CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <!-- Plyr CSS -->
    <link rel="stylesheet" href="https://cdn.plyr.io/3.7.8/plyr.css" />
    <!-- Favicon -->
    <link rel="icon" href="/media/videovault-icon.svg">
    <style>
        :root {
            --primary-color: #333;
            --secondary-color: #666;
            --border-color: #ddd;
            --button-color: #4285f4;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            color: var(--primary-color);
            line-height: 1.5;
            margin: 0;
            padding: 0;
            background: #fff;
        }

        /* 导航栏样式 */
        .navbar {
            border-bottom: 1px solid var(--border-color);
            padding: 1rem 0;
            margin-bottom: 2rem;
        }

        .navbar-brand {
            display: flex;
            align-items: center;
            font-size: 1.25rem;
            color: var(--primary-color) !important;
            text-decoration: none;
            transition: transform 0.3s ease;
        }

        .navbar-brand:hover {
            transform: scale(1.05);
        }

        .navbar-brand svg {
            margin-right: 0.5rem;
        }

        /* 视频列表样式 */
        .group-header {
            font-size: 1.25rem;
            font-weight: bold;
            color: #1a9cfb;
            margin: 2rem 0 1rem;
            padding: 0 0 0.5rem 0;
            position: relative;
            margin-left: -2em;
            margin-right: -2em;
        }

        .group-header::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: 0;
            width: 100%;
            border-bottom: 1px dashed var(--border-color);
        }

        .video-item {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding: 1rem;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .video-item:hover {
            transform: scale(1.02);
            background-color: rgba(66, 133, 244, 0.05);
        }

        .video-info {
            flex: 1;
            padding-right: 2rem;
        }

        .video-title {
            font-size: 1.1rem;
            color: var(--primary-color);
            margin: 0 0 0.5rem;
        }

        .video-description {
            color: var(--secondary-color);
            font-size: 0.9rem;
            margin: 0;
        }

        .watch-button {
            background-color: #3bd557;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
            cursor: pointer;
            text-decoration: none;
            white-space: nowrap;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .watch-button:hover {
            color: white;
            text-decoration: none;
            transform: scale(1.05);
            background-color: #d5873b;
        }

        /* 视频播放页样式 */
        .video-wrapper {
            display: flex;
            justify-content: center;
            width: 100%;
            margin-bottom: 2rem;
        }

        .video-container {
            background-color: #000;
            width: 100%;
            max-width: 1280px;
            line-height: 0;
            /* 移除容器底部的额外空间 */
            font-size: 0;
            /* 确保没有文字间距影响 */
        }

        video {
            width: 100%;
            max-width: 100%;
            height: auto;
            display: block;
            /* 移除视频元素底部的额外空间 */
        }

        .video-content {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .video-content h1 {
            font-size: 1.5rem;
            margin: 1rem 0;
        }

        .video-content p {
            color: var(--secondary-color);
            font-size: 1rem;
            margin: 1rem 0;
        }

        /* 全屏模式修复 */
        .plyr--fullscreen-fallback,
        .plyr--fullscreen {
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            right: 0 !important;
            bottom: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            max-width: 100vw !important;
            max-height: 100vh !important;
            z-index: 9999 !important;
            background: #000 !important;
            margin: 0 !important;
            padding: 0 !important;
            overflow: hidden !important;
        }
        
        .plyr--fullscreen .plyr__video-wrapper,
        .plyr--fullscreen-fallback .plyr__video-wrapper {
            position: absolute !important;
            top: 0 !important;
            left: 0 !important;
            width: 100% !important;
            height: 100% !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
            min-width: 100% !important;
            min-height: 100% !important;
            transform: none !important;
        }
        
        .plyr--fullscreen video,
        .plyr--fullscreen-fallback video {
            width: 100% !important;
            height: 100% !important;
            max-width: 100% !important;
            max-height: 100% !important;
            object-fit: contain !important;
            position: absolute !important;
            top: 0 !important;
            left: 0 !important;
            right: 0 !important;
            bottom: 0 !important;
            margin: auto !important;
        }
        
        /* 确保控制条在全屏模式下正确显示 */
        .plyr--fullscreen .plyr__controls,
        .plyr--fullscreen-fallback .plyr__controls {
            position: absolute !important;
            bottom: 0 !important;
            left: 0 !important;
            right: 0 !important;
            z-index: 10 !important;
        }

        /* 页脚样式 */
        .footer {
            text-align: center;
            padding: 2rem 0;
            color: var(--secondary-color);
            font-size: 0.9rem;
            border-top: 1px solid var(--border-color);
            margin-top: 3rem;
        }

        /* 响应式调整 */
        @media (max-width: 768px) {
            .video-item {
                flex-direction: row;
                /* 保持水平布局 */
                padding: 1rem;
                margin: 0 1rem;
                /* 与分组标题对齐 */
            }

            .video-info {
                padding-right: 1rem;
                /* 恢复右边距 */
                margin-bottom: 0;
                /* 移除底部边距 */
                flex: 1;
                /* 让内容区域占据剩余空间 */
            }

            .watch-button {
                width: auto;
                /* 恢复自动宽度 */
                white-space: nowrap;
                /* 确保按钮文字不换行 */
                align-self: center;
                /* 垂直居中对齐 */
            }

            .search-container {
                margin: 0 1rem;
            }

            .group-header {
                margin-left: 0;
                margin-right: 0;
                padding: 0 1rem 0.5rem 1rem;
            }

            .group-header::after {
                left: 1rem;
                width: calc(100% - 2rem);
            }
        }
    </style>
    <% if (typeof isVideoPage !=='undefined' && isVideoPage) { %>
        <script>
            // 禁止F12和右键菜单
            document.addEventListener('keydown', function (e) {
                if (e.keyCode === 123) { // F12 key
                    e.preventDefault();
                    return false;
                }
            });

            document.addEventListener('contextmenu', function (e) {
                e.preventDefault();
                return false;
            });

            // 检测是否为移动设备
            const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
            
            // 创建Plyr播放器实例
            const player = new Plyr('video', {
                controls: ['play', 'progress', 'current-time', 'mute', 'volume', 'fullscreen'],
                fullscreen: { 
                    enabled: true,
                    fallback: true,
                    iosNative: true // 在iOS设备上使用原生全屏
                }
            });
            
            // 处理移动设备全屏
            const requestFullscreenForMobile = () => {
                const video = player.elements.container;
                
                if (video.requestFullscreen) {
                    video.requestFullscreen();
                } else if (video.webkitRequestFullscreen) {
                    video.webkitRequestFullscreen();
                } else if (video.webkitEnterFullscreen) { // iOS支持
                    video.webkitEnterFullscreen();
                } else if (video.msRequestFullscreen) {
                    video.msRequestFullscreen();
                }
            };

            // 获取视频容器
            const videoWrapper = document.querySelector('.video-container');
            const videoElement = document.querySelector('video');
            
            // 处理移动设备横屏
            const handleOrientationChange = () => {
                if (isMobile && player.fullscreen.active) {
                    // 强制横屏时重新调整视频尺寸
                    setTimeout(() => {
                        player.resize();
                        videoElement.style.width = '100%';
                        videoElement.style.height = '100%';
                        videoElement.style.objectFit = 'contain';
                    }, 100);
                }
            };
            
            // 监听屏幕方向变化
            window.addEventListener('orientationchange', handleOrientationChange);
            
            // 全屏模式处理函数
            const handleFullscreenChange = () => {
                // 如果是移动设备，尝试使用原生全屏
                if (isMobile && player.fullscreen.active) {
                    requestFullscreenForMobile();
                }
                
                if (document.fullscreenElement || 
                    document.webkitFullscreenElement || 
                    document.mozFullScreenElement ||
                    document.msFullscreenElement) {
                    // 进入全屏
                    videoElement.style.width = '100%';
                    videoElement.style.height = '100%';
                    videoElement.style.objectFit = 'contain';
                    videoElement.style.maxWidth = 'none';
                    videoElement.style.maxHeight = 'none';
                } else {
                    // 退出全屏
                    setTimeout(() => {
                        initVideoLayout();
                    }, 100);
                }
                player.resize();
            };
            
            // 监听全屏变化事件
            document.addEventListener('fullscreenchange', handleFullscreenChange);
            document.addEventListener('webkitfullscreenchange', handleFullscreenChange);
            document.addEventListener('mozfullscreenchange', handleFullscreenChange);
            document.addEventListener('MSFullscreenChange', handleFullscreenChange);
            
            // 全局视频初始化逻辑
            const initVideoLayout = () => {
                if (!videoWrapper || !videoElement) return;
                
                // 重置容器样式
                videoWrapper.style.width = '100%';
                videoWrapper.style.height = 'auto';
                videoWrapper.style.display = 'block';
                
                // 重置视频元素样式
                videoElement.style.width = '100%';
                videoElement.style.height = 'auto';
                videoElement.style.objectFit = 'contain';
                videoElement.style.maxWidth = '100%';
                
                // 强制重绘
                player.resize();
                
                // 打印尺寸信息以便调试
                console.log('视频布局初始化完成', {
                    videoWidth: videoElement.videoWidth,
                    videoHeight: videoElement.videoHeight,
                    containerWidth: videoWrapper.offsetWidth,
                    containerHeight: videoWrapper.offsetHeight
                });
            };
            
            // 添加全局尺寸监听
            window.addEventListener('resize', () => {
                if (!player.fullscreen.active) {
                    requestAnimationFrame(initVideoLayout);
                }
            });
            
            // 播放器事件监听
            player.on('ready', () => {
                console.log('视频播放器初始化完成');
                videoWrapper.dataset.playerReady = 'true';
                initVideoLayout();
                
                // 使用ResizeObserver监听容器大小变化
                if (window.ResizeObserver) {
                    new ResizeObserver(() => {
                        if (!player.fullscreen.active) {
                            requestAnimationFrame(initVideoLayout);
                        }
                    }).observe(videoWrapper);
                }
            });
            
            // 视频元数据加载完成后调整尺寸
            player.on('loadedmetadata', () => {
                initVideoLayout();
            });
            
            // 全屏事件处理
            player.on('enterfullscreen', () => {
                if (isMobile) {
                    // 移动设备使用原生全屏
                    requestFullscreenForMobile();
                    
                    // 尝试锁定横屏方向
                    if (screen.orientation && screen.orientation.lock) {
                        screen.orientation.lock('landscape').catch(() => {
                            // 某些设备可能不支持锁定方向，忽略错误
                            console.log('设备不支持锁定屏幕方向');
                        });
                    }
                }
                
                videoElement.style.width = '100%';
                videoElement.style.height = '100%';
                videoElement.style.objectFit = 'contain';
                videoElement.style.maxWidth = 'none';
                videoElement.style.maxHeight = 'none';
                player.resize();
            });
            
            player.on('exitfullscreen', () => {
                if (isMobile) {
                    // 退出全屏时解除屏幕方向锁定
                    if (screen.orientation && screen.orientation.unlock) {
                        screen.orientation.unlock();
                    }
                }
                setTimeout(initVideoLayout, 100);
            });
            
            // 监听播放开始，为iOS设备准备
            player.on('play', () => {
                if (isMobile && /iPad|iPhone|iPod/.test(navigator.userAgent)) {
                    videoElement.webkitEnterFullscreen = videoElement.webkitEnterFullscreen || function() {};
                }
            });
            
            // 初始化播放器
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initVideoLayout);
            } else {
                initVideoLayout();
            }
            
            // 处理动态加载内容
            document.addEventListener('video-loaded', initVideoLayout);
        </script>
        <% } %>
</head>

<body>
    <div class="container">
        <nav class="navbar">
            <div class="container">
                <a href="/" class="navbar-brand">
                    <% const defaultLogo="/media/videovault-icon.svg" ; const defaultName="VideoVault" ; const
                        logoSrc=(typeof siteBrand !=='undefined' && siteBrand && siteBrand.logo) ? siteBrand.logo :
                        defaultLogo; const brandName=(typeof siteBrand !=='undefined' && siteBrand && siteBrand.name) ?
                        siteBrand.name : defaultName; %>
                        <img src="<%= logoSrc %>" alt="<%= brandName %>" width="24" height="24"
                            style="margin-right: 0.5rem;">
                        <span style="color: #fd7e0d;">
                            <%= brandName %>
                        </span>
                </a>
            </div>
        </nav>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <%- body %>
            </div>
        </div>

        <footer class="footer">
            <p>&copy; <%= new Date().getFullYear() %>
                    <% if (siteBrand && siteBrand.name) { %>
                        <%= siteBrand.name %> by <% } %><a href="https://github.com/hcllmsx/VideoVault"
                                    style="color: inherit; text-decoration: none; font-weight: bold;">VideoVault</a>.
                                All rights reserved.</p>
        </footer>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="/js/bootstrap.bundle.min.js"></script>
    <!-- Plyr JS -->
    <script src="https://cdn.plyr.io/3.7.8/plyr.polyfilled.js"></script>
</body>

</html>