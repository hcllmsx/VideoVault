<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'VideoVault' %></title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22><%= typeof isVideoPage !== 'undefined' && isVideoPage ? '📷' : '❤️' %></text></svg>">
    <style>
        :root {
            --primary-color: #333;
            --secondary-color: #666;
            --border-color: #ddd;
            --button-color: #4285f4;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            color: var(--primary-color);
            line-height: 1.5;
            margin: 0;
            padding: 0;
            background: #fff;
        }

        /* 导航栏样式 */
        .navbar {
            border-bottom: 1px solid var(--border-color);
            padding: 1rem 0;
            margin-bottom: 2rem;
        }

        .navbar-brand {
            display: flex;
            align-items: center;
            font-size: 1.25rem;
            color: var(--primary-color) !important;
            text-decoration: none;
            transition: transform 0.3s ease;
        }

        .navbar-brand:hover {
            transform: scale(1.05);
        }

        .navbar-brand svg {
            margin-right: 0.5rem;
        }

        /* 视频列表样式 */
        .group-header {
            font-size: 1.25rem;
            font-weight: bold;
            color: #1a9cfb;
            margin: 2rem 0 1rem;
            padding: 0 0 0.5rem 0;
            position: relative;
            margin-left: -4em;
        }

        .group-header::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: 0;
            width: 100%;
            border-bottom: 1px dashed var(--border-color);
        }

        .video-item {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding: 1rem 0;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .video-item:hover {
            transform: scale(1.02);
            background-color: rgba(66, 133, 244, 0.05);
        }

        .video-info {
            flex: 1;
            padding-right: 2rem;
        }

        .video-title {
            font-size: 1.1rem;
            color: var(--primary-color);
            margin: 0 0 0.5rem;
        }

        .video-description {
            color: var(--secondary-color);
            font-size: 0.9rem;
            margin: 0;
        }

        .watch-button {
            background-color: #3bd557;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
            cursor: pointer;
            text-decoration: none;
            white-space: nowrap;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .watch-button:hover {
            color: white;
            text-decoration: none;
            transform: scale(1.05);
            background-color: #d5873b;
        }

        /* 视频播放页样式 */
        .video-wrapper {
            display: flex;
            justify-content: center;
            width: 100%;
            margin-bottom: 2rem;
        }

        .video-container {
            background-color: #000;
            width: 100%;
            max-width: 1280px;
            line-height: 0; /* 移除容器底部的额外空间 */
            font-size: 0; /* 确保没有文字间距影响 */
        }

        video {
            width: 100%;
            max-width: 100%;
            height: auto;
            display: block; /* 移除视频元素底部的额外空间 */
        }

        .video-content {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .video-content h1 {
            font-size: 1.5rem;
            margin: 1rem 0;
        }

        .video-content p {
            color: var(--secondary-color);
            font-size: 1rem;
            margin: 1rem 0;
        }

        /* 页脚样式 */
        .footer {
            text-align: center;
            padding: 2rem 0;
            color: var(--secondary-color);
            font-size: 0.9rem;
            border-top: 1px solid var(--border-color);
            margin-top: 3rem;
        }

        /* 响应式调整 */
        @media (max-width: 768px) {
            .video-item {
                flex-direction: column;
            }

            .video-info {
                padding-right: 0;
                margin-bottom: 1rem;
            }

            .watch-button {
                width: 100%;
                text-align: center;
            }

            .search-container {
                margin: 0 1rem;
            }

            .group-header {
                margin-left: 0; /* 移动端不需要负边距 */
                padding: 0 1rem 0.5rem 1rem; /* 添加合适的内边距 */
            }

            .group-header::after {
                left: 1rem; /* 调整下划线位置 */
                width: calc(100% - 2rem); /* 调整下划线宽度 */
            }
        }
    </style>
    <% if (typeof isVideoPage !== 'undefined' && isVideoPage) { %>
    <script>
        // 禁止F12和右键菜单
        document.addEventListener('keydown', function(e) {
            if (e.keyCode === 123) { // F12 key
                e.preventDefault();
                return false;
            }
        });
        
        document.addEventListener('contextmenu', function(e) {
            e.preventDefault();
            return false;
        });
    </script>
    <% } %>
</head>
<body>
    <div class="container">
        <nav class="navbar">
            <div class="container">
                <a href="/" class="navbar-brand">
                    <% if (process.env.SITE_BRAND) { %>
                        <img src="<%= JSON.parse(process.env.SITE_BRAND).logo %>" alt="Logo" width="24" height="24" style="margin-right: 0.5rem;">
                        <%= JSON.parse(process.env.SITE_BRAND).name %>
                    <% } else { %>
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z" fill="#ff0000"/>
                        </svg>
                        <span style="color: #fd7e0d;">VideoVault</span>
                    <% } %>
                </a>
            </div>
        </nav>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <%- body %>
            </div>
        </div>

        <footer class="footer">
            <p>&copy; <%= new Date().getFullYear() %> <% if (process.env.SITE_BRAND) { %><%= JSON.parse(process.env.SITE_BRAND).name %><% } else { %><a href="https://github.com/hcllmsx/VideoVault" style="color: inherit; text-decoration: none; font-weight: bold;">VideoVault</a><% } %>. All rights reserved.</p>
        </footer>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>